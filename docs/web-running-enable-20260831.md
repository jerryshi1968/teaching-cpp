# 接入执行器并开放网页运行

2026-08-31 最新结果：用户于 13:46:03Z～13:46:09Z 完成网页运行启用，网站账号连接、数据库调度锁、HTTP/HTTPS 开关及原站状态核验通过。只重启 C++ 网站后端；p5.js、Apache、执行服务、用户管理器及 PM2 保存记录保持。私有备份 web-running-enable-hTFDVJ，日志 web-running-enable-bcf281dccc38.log。随后截图显示“两数之和”输入 `17 30`、输出 `a + b = 47`，状态“运行完成”，首次真实网页运行通过。现在转到 [课堂功能检查](classroom-acceptance-20260831.md)，下方启用步骤保留作历史记录，不重跑脚本。

2026-08-31，用户回报内部执行服务安装成功：`13:16:52Z`～`13:17:05Z` 完成认证、实际 C++17 输入输出、CPU 超时、主动返回 137 对照、重复提交及服务重启后记录恢复检查。执行服务由 cpp-runner 普通账号运行，仅监听 `127.0.0.1:5280`，已设置开机启动；没有重启机器，不能据此认为机器重启恢复已经实测。

成功记录：`/var/www/teaching-cpp-backend/backups/runner-service-start-zMMEp9`。日志：`/var/www/teaching-cpp-backend/logs/runner-service-start-bb4ac3f67cec.log`。p5.js、C++ 网站、用户管理器及账号总限额保持，网页运行当时仍关闭。

本步骤只更新 C++ 网站配置，重启一个网站后端进程，开放“保存并运行”。**不会修改 Apache，不重启 p5.js、执行服务或用户管理器，不迁移数据库，不修改应用源码或前端。**

## 上传与执行

先保存浏览器中正在编辑的内容，等待脚本结束后再操作网页。C++ 网站后端会有几秒钟的重启间隔，p5.js 保持运行。

用 WinSCP 只上传一个新文件，保留 LF 换行：

| 本地文件 | 服务器完整路径 |
| --- | --- |
| `G:\teaching-cpp\deploy\enable-web-running-20260831.sh` | `/var/www/teaching-cpp-backend/deploy/enable-web-running-20260831.sh` |

在 PuTTY 原来的 root 会话执行一次：

```bash
bash /var/www/teaching-cpp-backend/deploy/enable-web-running-20260831.sh
```

无需手工编辑 `.env`、复制密钥、安装软件或上传整个项目。保留原来的启动脚本和部署目录，新脚本会复用其中已验证的只读检查函数，不重跑旧流程。

## 操作范围与检查

1. 核对内部服务成功记录、已通过的三个 HTTP 测试、当前服务身份、固定镜像和代码摘要，检查 p5.js 类别隔离模型及两个网站状态。
2. 在原资源限额内检查镜像库及零容器状态；读取 `.env.runner` 中已生成的内部密钥，不重新生成或公开它。
3. 在内存中生成候选网站配置。只填写 `RUNNER_TOKEN`、`CPP_COMPILER_IMAGE`，把 `CPP_RUN_ENABLED` 从 `false` 改为 `true`。原密码、其他设置、注释、引号、空格和换行保留。镜像配置原来若只有注释，会保留该注释并追加一行实际配置。
4. 在修改配置前，以 cpp-web 身份读取候选配置，通过原 `RunWorker.request()` 访问执行器健康接口，并执行数据库只读查询。确认结构已迁移、没有待处理任务，也没有其他进程持有调度锁。事务控制使用文本协议，避免之前出现的 `ER_UNSUPPORTED_PS`。
5. 将网站原 `.env` 和 PM2 保存记录备份到私有目录，原子替换网站配置，然后只重启 PM2 中已有的 `teaching-cpp-backend`。
6. 检查网站实际普通用户身份、5180 本机监听、HTTP/HTTPS 运行开关、未登录访问拒绝、前端首页一致性、网站账号到执行器的连接和数据库调度锁。再次确认 p5.js、执行服务、Apache 配置和总限额不变。

网站在启动时会取得名为 `teaching_p5js:cpp-worker` 的 MySQL 会话锁，以避免重复调度。脚本只通过 `IS_USED_LOCK()` 查询锁是否已被持有，不以检查程序占用这个锁。[MySQL 8.4 锁函数说明](https://dev.mysql.com/doc/refman/8.4/en/locking-functions.html)

脚本检查 PM2 没有覆盖 `.env` 中的运行配置。它不使用 `--update-env`，不把新密钥写进 PM2 配置，不执行 `pm2 save`；原来的启动配置会在进程启动时重新读取网站 `.env`。[PM2 环境变量说明](https://pm2.keymetrics.io/docs/usage/environment/)

现有配置仍为：全局一次执行一个任务、最多 20 个未结束任务、每人最多一个未结束任务；运行历史保留一天，每人缓存上限 100 MiB。cpp-runner 账号合计仍限制为 1 GiB 内存、1 核 CPU、256 个进程/线程、swap 0。本步骤不做课堂负载测试。

## 成功后的网页检查

脚本明确显示“网页 C++ 运行已启用”后：

1. 刷新 C++ 网页，新建练习并选择 **两数之和** 示例。
2. 在“测试输入”中填写 `12 30`，点击 **保存并运行**。
3. 预期状态变为“运行完成”，输出为 **42**。刷新后重新打开，代码和输入应保留。
4. 确认 p5.js 原有作品仍能保存和预览。

把终端完整输出和网页结果发回。先用你自己的账号测试，不急着给全班分发。该脚本不会伪造登录、创建测试账号、向业务数据库塞入测试作品；真实浏览器保存、队列调度与结果回显必须在网页中验证。

## 失败处理与记录

若修改后的检查未通过，脚本会尝试停止这一个 C++ 网站后端，恢复原 `.env`，再启动为 **保存开启、运行关闭**。它不恢复或覆盖整个数据库，也不删除此期间可能保存的作品。内部执行服务保持运行；若存在尚未结束的业务任务记录，会报告数量并保留，不能把停止调度等同于任务已经停止。

如果恢复本身失败，会明确报告“自动恢复未确认”。发回完整输出即可，不重复执行、不还原数据库、不删除锁文件或旧记录。

- 日志：`/var/www/teaching-cpp-backend/logs/web-running-enable-随机名.log`
- 私有备份和检查记录：`/var/www/teaching-cpp-backend/backups/web-running-enable-随机名`
- 防重复锁：`/var/www/teaching-cpp-backend/backups/web-running-enable.lock`

备份中的 `env.before` 含数据库密码，`pm2-saved.before` 也可能含私有环境信息。不要发送或公开这些文件；也不要发送 `.env`、`.env.runner` 或任何密钥。

## 验证边界

本地检查覆盖配置原文保留、重复项和错误旧值拒绝、PM2 覆盖项、只读数据库协议和调度锁状态、部分失败恢复及 Windows 上拒绝 Linux 入口。本地没有生产 MySQL/Podman，不能把模拟检查当作服务器运行成功。

新增 17 项检查通过，包括模拟 Linux 下网站账号检查的启用前、启用后和恢复三个分支，以及身份、执行器或数据库异常时拒绝继续且不输出凭据。当前完整测试为 291 项，290 项通过、1 项可选 MySQL 跳过；Bash/Node 语法和 UTF-8 无 BOM/LF 检查通过。17 个已交付部署脚本和 46 个原清单项摘要保持。

```text
enable-web-running-20260831.sh
SHA256: f4c22f406c888524ba3443c538a124b5434929c7926f61043aefa8f3cc0093e7
```

交付时，18 项容器验收和内部服务检查已经通过，本次网站接入及网页首次完整运行尚待确认。用户随后回报启用日志通过，并提供 17 + 30 得到 47 的成功截图；刷新后的历史/快照和课堂功能另行验收，不能从一次运行成功推定全部通过。
