# 目标：/var/www/teaching-cpp-backend/.env.runner；仅 cpp-runner 用户可读。
# 不包含网站数据库密码、JWT 密钥或学生登录令牌。
RUNNER_PORT=5280
RUNNER_DATA_ROOT=/var/www/teaching-cpp-runner/runner-data
# 必须在后续隔离验证步骤中填写，留空时执行服务会拒绝启动。
RUNNER_TOKEN=
# CPP_COMPILER_IMAGE=sha256:<64位真实镜像ID，与网站配置一致>
