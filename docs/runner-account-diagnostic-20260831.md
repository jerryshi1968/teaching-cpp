# 执行账号准备失败后的排查

2026-08-31 最新状态：网页运行已启用，网站账号连接和调度锁检查通过；用户截图确认“两数之和”输入 17 30、输出 a + b = 47，首次真实网页运行成功。只重启了 C++ 网站后端，p5.js、Apache、执行服务和用户管理器保持。下一步按 [课堂功能检查](classroom-acceptance-20260831.md) 验证刷新与历史、原站及测试班流程，不再执行安装或启用脚本。下方旧步骤保留作历史记录，不重跑。

以下为诊断脚本交付时的记录，诊断已完成，不必重复执行。

2026-08-31 用户回报 `prepare-runner-account-20260831.sh` 输出两行错误，未保留原文。当前只能确认该步没有报告成功；错误位置、账号与目录是否已部分创建均待核实。不要重跑原准备脚本，也不要删除账号、目录或映射。

原脚本在完成早期预检后才创建操作日志，因此早期失败可能没有留下日志。此次不推定具体原因，另提供一个诊断脚本：寻找现有操作日志和备份阶段、检查账号与目录是否存在、列出预检所用文件的类型和权限、检查工具版本及两个网站的健康状态。

## 上传并执行

1. 用 WinSCP 将本机 `G:\teaching-cpp\deploy\diagnose-runner-account-20260831.sh` 上传到服务器 `/var/www/teaching-cpp-backend/deploy/`。这是新文件，不覆盖原准备脚本。
2. 在 PuTTY 的 root 会话执行：

```bash
bash /var/www/teaching-cpp-backend/deploy/diagnose-runner-account-20260831.sh
```

请发回终端输出。本次诊断还会自动保存到 `/var/www/teaching-cpp-backend/logs/runner-account-diagnostic-*.log`，权限为 root 私有；具体文件名在输出首尾显示。如果日志目录不可用，会继续在终端显示结果并明确提醒无法保存。

## 操作范围

- 只新增一份诊断日志，不创建或修改系统账号、用户映射、应用目录、配置或服务；网站健康请求可能产生常规 Apache 访问日志。
- 不执行原准备流程，不初始化 Podman，不拉取镜像，不启动容器或执行服务，不重启网站，不开启 C++ 编译运行。
- 不读取 `.env`、shadow/gshadow 或备份密码文件的内容，不输出完整 PM2 环境或失败命令的原始 stderr。现有操作日志只摘取阶段、错误码和完成标记。
- 路径检查只显示类型、权限、所有者数字编号和链接目标。符号链接或硬链接提示用于判断是否不符合原脚本的预检条件，并不说明系统文件本身损坏；不要据此改权限或替换文件。

原准备脚本及已交付安装材料保持不变。此次新增 5 项诊断数据过滤测试，全部通过；完整本地检查共 133 项，132 项通过、1 项可选 MySQL 跳过。Bash 和内嵌 Node 语法、UTF-8 无 BOM / LF 检查通过。这些是 Windows 本地验证，没有连接服务器，服务器诊断结果仍待回报。

诊断脚本 SHA256：`4854c2cac37e6655c7134663a91c9419c3c50c63eb0809149010cc48ec915a12`。
