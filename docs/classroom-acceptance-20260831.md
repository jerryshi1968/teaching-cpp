# 网页运行通过后的课堂功能检查

2026-08-31，用户回报网站运行启用成功，并提供真实网页运行截图。基础部署与首次网页编译运行已经打通；本阶段不再安装软件、重跑部署脚本或修改服务器配置，也不做课堂并发压测。

## 已收到的验收结果

| 项目 | 已有证据与结论 |
| --- | --- |
| 网站接入执行器 | 13:46:03Z～13:46:09Z 的启用日志通过，网站账号认证连接及数据库调度锁检查成功 |
| 配置变动范围 | 仅更新网站内部密钥、固定镜像和运行开关，仅重启 C++ 网站后端；日志确认 p5.js、Apache、执行服务、用户管理器及 PM2 保存记录保持 |
| 真实网页运行 | 截图中“两数之和”输入为 `17 30`，输出 `a + b = 47`，显示“已保存”和“运行完成” |
| 代码与输入刷新恢复 | 此前新建、保存、刷新重开已由用户确认；本次运行后的历史结果和运行快照仍待浏览器检查 |
| 原站 | 此前 p5.js 保存、预览和运行已由用户确认；本次启用后自动健康检查通过，浏览器回归尚待回报 |
| 课堂功能 | 班级和学生列表已由用户确认；实际模板分发、学生修改运行和教师只读查看尚待验证 |
| 后续运维 | 开机启动设置已有检查，真实整机重启、备份恢复演练及课堂负载尚未验证，不据此宣称全部完成 |

本次服务器记录：

- 日志：`/var/www/teaching-cpp-backend/logs/web-running-enable-bcf281dccc38.log`
- 私有备份与检查记录：`/var/www/teaching-cpp-backend/backups/web-running-enable-hTFDVJ`

上述备份可能含数据库密码和内部密钥，不发送或公开。启用脚本的 `result.json` 中 `browserRunVerified: false` 是脚本结束时的实际状态；后续用户截图作为独立浏览器证据记录在这里，不改写服务器历史记录。

## 先完成两个简短检查

1. 刷新 C++ 页面并重新打开“两数之和”。确认代码和输入 `17 30` 仍在；在“历史”中找到刚才的成功记录，点开并查看“本次运行的代码与输入”，应对应产生 `a + b = 47` 的那次提交。历史缓存最多保留一天，不要求过期记录仍存在。
2. 打开自己原有的 p5.js 作品，确认仍能保存、预览或运行。无需改动原作品内容来证明服务正常。

## 再用测试班验证课堂流程

**“分发到班级”会给该班每位学生创建独立副本，当前弹窗不提供只选一名学生的选项。** 先用只含自己测试学生账号的班级，不向现有正式班级试分发。如果没有这样的班级或可使用的测试学生账号，先反馈，不调整真实学生的班级归属。

班级、注册和入班仍使用原 p5.js 平台现有功能，有班级码即可加入，不新增教师审批流程。教师和学生分别使用普通窗口与无痕窗口，避免同一浏览器会话相互切换账号；学生在独立窗口登录原平台后打开 C++ 页面，先确认右上角为学生身份。

1. **教师分发。** 打开自己的“两数之和”，点右上角“分发到班级”，核对所选测试班，点一次“保存并分发”。预期提示分发人数与测试班人数一致，学生获得副本。不要再次主动分发以“确认成功”，主动再次分发会新增副本。
2. **学生修改并运行。** 在学生窗口刷新 C++ 工作台，打开收到的练习。把测试输入改为 `5 8`，点“保存并运行”，预期输出 `a + b = 13`。刷新重开后，学生自己的输入应仍为 `5 8`。
3. **教师只读查看。** 在教师窗口进入“我的课堂”，选择该测试班，点测试学生的“查看练习”，打开刚才的副本。应显示“只读查看”，看到学生已保存的代码和输入；不能编辑保存、运行或停止学生任务。
4. **确认副本独立。** 教师回到自己的练习，原“两数之和”的输入仍应为 `17 30`，不应被学生改成 `5 8`；学生其他已有练习也不应被覆盖。

这些检查会通过正常网页操作产生少量测试副本与运行记录，不伪造身份或直接修改数据库。跨班级和异常接口权限、全班并发及容量测试仍是另外的检查，不能仅凭这一名学生的正常流程通过就推定全部通过。

## 回报方式

先回报“刷新与历史正常 / p5.js 正常”，并说明是否已有仅含测试账号的班级。课堂检查完成后再回报“分发正常 / 学生运行正常 / 教师只读正常 / 副本独立正常”。若有异常，给出操作步骤、错误提示或截图即可，不发送账号密码、浏览器登录令牌或服务器密钥。

本页只记录本次状态和下一步操作，不要求上传任何新文件或在 PuTTY 执行命令。同源风险事项仍由原 p5.js 项目处理，本次不修改或重新安排那部分工作。
